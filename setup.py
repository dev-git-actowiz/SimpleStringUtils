from setuptools import setup, find_packages

setup(
    name="SimpleStringUtils",
    version="0.1.0",
    author="Ankit",
    description="A simple library to work with strings",
    packages=find_packages(),
    python_requires=">=3.6",
)
